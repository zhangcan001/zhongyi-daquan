# Release Notes

## v0.1.2-test

测试发布名：`v0.1.2-test`  
应用版本：`0.1.2`

`v0.1.2-test` 聚焦搜索和阅读体验，把已导入的人纪资料从“能导入”推进到“更好查、好看、好用”。本版本仍是本地学习资料库测试版，不随 Release 发布私人数据包，不上传安装包，不包含 raw PDF。

### 已完成

- 搜索体验优化：首页提供更醒目的全局搜索框，支持中药、方剂、针灸、原典和注解筛选。
- 搜索结果优化：按中药、方剂、穴位、经络、原典章节、原典条文、注解资料和其他分组展示。
- 详情页阅读体验优化：主信息、正文、类型专属字段、资料注解、个人备注分区展示。
- 注解独立展示：`knowledge_annotations` 不再和主条目混成一段，支持来源、页码、标签和折叠查看。
- 最近查看：打开详情后记录本地最近查看，首页显示最近打开条目。
- 收藏：支持收藏/取消收藏，并在首页展示我的收藏入口。
- 个人备注：支持本地轻量备注保存和删除，不参与医疗建议。
- 首页学习工作台：提供全局搜索、快捷入口、数据概览、最近查看、收藏和导入历史摘要。
- 导入历史入口：保留 Smart Import Center 的报告和回滚入口，首页仅展示业务摘要。
- 导入回滚能力延续：导入运行记录、报告和回滚能力继续可用。

### 安全边界

- 不真实调用 AI。
- 不在线问诊。
- 不自动诊断。
- 不自动开方。
- 不提供针灸操作指导。
- 私人数据包不随 Release 发布。

### 打包命令

```powershell
npm run tauri:build
```

### 测试发布安装包命名

Tauri/NSIS 使用应用版本 `0.1.2` 生成安装包后，测试发布资产复制为：

```text
release-assets/zhongyi-daquan_0.1.2-test_x64-setup.exe
```

### 已知限制

- Playwright 截图级验证仍未完成。
- 真实全量库之前 `knowledge_annotations = 0`；本轮补充使用 `data-seed/renji-samples` 做本地样例注解详情视觉验收。

## v0.1-alpha

发布时间准备版，完成源码构建与 Windows 桌面安装包验证。

## v0.1-alpha-package

基于 `v0.1-alpha`，完成本地打包验证、启动验证和发布文档补齐。

版本口径：当前发布收口统一使用 `v0.1-alpha-package`；源码包版本仍为 `0.1.0`。

### 已完成

- 全量检查通过：`check_all.ps1`、前端检查、前端构建、Rust fmt、cargo check、cargo test。
- 生产打包命令通过：`npm run tauri:build`。
- 已确认 Windows 可执行文件与 NSIS 安装包产物路径。
- 已补齐 README、打包说明和发布检查清单。
- 已补充经典数据导入说明，完整经典精校数据包不直接进入仓库。
- 已完成回归数据性能验证：10,000 条知识、50,000 条关系下，指定五个关键词搜索均小于 500ms。

### 启动命令

```powershell
npm run tauri:dev
```

### 打包命令

```powershell
npm run tauri:build
```

### 打包产物

```text
src-tauri/target/release/zhongyi-daquan.exe
src-tauri/target/release/bundle/nsis/中医大全_0.1.0_x64-setup.exe
```

### 当前限制

- 不真实调用 AI。
- 不在线问诊。
- 不自动诊断。
- 不自动开方。
- 不创建 GitHub Release。
- 不上传安装包。

### 已完成

- 本地首页、知识库、表格录入、搜索面板、导入示例入口。
- AI 默认关闭，仅保留本地占位与设置入口。
- 本地数据库目录与 SQLite 文件首次启动可自动创建。
- 自动检查通过：前端检查、前端构建、Rust fmt、cargo check、cargo test。
- 已验证 Windows NSIS 安装包生成与启动。

### 当前限制

- 不接入真实 AI。
- 不联网。
- 不做人体穴位图。
- 不做健康档案。
- 不做医案系统。
- 不包含 v0.2 功能。

### 发布声明

本软件仅用于中医知识学习、资料整理与本地记录，不构成医疗诊断、治疗建议或处方依据。
